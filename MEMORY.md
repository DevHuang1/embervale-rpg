# Embervale — Implementation Notes

- The game uses an isometric diorama rather than a pixel-art sprite field to honor the generated visual target while keeping gameplay legible and lightweight.
- Keep all generated originals outside the deployable project tree and reference only uploaded storage URLs.
- The hero’s lantern and the beacon should remain the strongest warm lights in the scene; do not add competing bright colors.
- Browser inspection confirmed the first implementation mounts correctly: the opening folio, illustrated ledger, live diorama, and touch controls are present. The visual pass then strengthened the amber lantern aura, objective readability, and wax-sealed engraved ledger framing.
- The deterministic `?demo` route completed the full quest, reaching the restored-beacon resolution at lantern level 2 and 35 Ember marks. Keyboard combat shortcuts now match the labelled 1 / 2 / 3 controls.
- The cursor-control upgrade now presents direct on-screen instructions: click any grove location to walk and click the Hushling to begin a chase-and-auto-strike engagement. The opening folio can be dismissed normally before interacting with the world.
- Initial browser interaction confirmed the cursor-control instruction surface but did not yet expose an auto-strike state after clicking the apparent enemy center; the picking path is under active verification.
- A fresh browser run dismisses the opening folio correctly and presents the Hushling unobstructed in the grove for the final selection test.
- Direct browser coordinate clicks did not produce the expected target-lock overlay in the captured state, so the final verification is switching to programmatic canvas event tracing rather than relying on coordinate translation alone.
- The preview canvas reports a 1280 × 1100 client rectangle even though browser screenshots are visually scaled; a direct pointer gesture has been dispatched using the unscaled enemy-center coordinates for the next state readback.
- After restarting with the direct canvas handler, the title folio again dismisses correctly and the live game world is ready for a fresh, unscaled pointer-coordinate selection check.
- A browser-side pointer trace has been attached to the canvas to confirm the actual coordinate values delivered by browser-driven clicks before the final interaction adjustment.
- Pointer tracing confirms the browser sends the visually selected Hushling location as `clientX 438 / clientY 341`; the game must translate this scaled browser coordinate to its 1280 × 1100 Babylon render surface before picking.
- The actual browser canvas, render buffer, and viewport are all 1280 × 1100; a true-coordinate direct pointer gesture at the Hushling’s expected screen position has been dispatched to validate normal browser input independent of the screenshot tool’s coordinate scaling.
- The development pick inspector reports no hit at either candidate screen coordinate, revealing that the procedural world meshes need explicit Babylon pickability before click navigation or target selection can resolve.
- Even with pickability enabled, the current ArcRotate/Babylon preview reports no screen-space pick hit through the browser harness. The control implementation is therefore moving to a deterministic map-space pointer command model that preserves player-facing click controls without relying on fragile preview pick behavior.
- The cursor-command layer now mounts an accessible, correctly placed Hushling target control in the live browser after the opening folio closes; this provides a robust user-facing click target for automatic engagement.
- Live browser verification confirms that selecting the Hushling starts the chase-and-auto-strike loop, reduces enemy and player warmth, awards 35 Ember marks on victory, and advances to the Ember Shard stage. An empty-grove pointer command has also been dispatched through the cursor-command layer to verify click-to-move routing.
- The enhanced build opens with a visible satchel, a Moss Tonic ready for use, the Cinder Warden class panel, and ready Cinder Lash/Mend Flame buttons. After dismissing the folio, the Hushling target surface remains available alongside cursor movement.
- Browser testing confirmed Hushling auto-strike victory, loot toast visibility, and reward updates to two Moss Tonics and one Hushling Thorn. Mend Flame restored the reduced warmth to full health and displayed its independent cooldown correctly.
- A final live browser check sent a cursor movement command through the upgraded UI and received the completion message “You reach the place your lantern marked,” confirming click-to-move remains functional alongside inventory and class systems.
- Error investigation found a blank Babylon canvas in both preview and production while the HUD remained healthy. TypeScript and production builds succeed, the canvas has a working WebGL2 context, and no runtime exception is reported. The leading diagnosis is that the full-screen overlay’s dark pseudo-element vignette is visually masking the scene; this will be reduced and split into a non-obscuring frame layer.
- Disabling the overlay pseudo-elements in the live preview did not reveal the forest, eliminating the HUD vignette as the primary cause. The rendering fault is therefore inside scene creation or render-loop startup rather than CSS stacking.
- Direct scene inspection confirms that the render loop is active, the storybook camera is active and ready, and 157 meshes are present; however, `scene.isReady()` remains false. The blank canvas is therefore caused by one or more scene assets failing Babylon readiness checks rather than a missing camera or stalled render loop.
- Every procedural mesh is blocked on an unready StandardMaterial effect. The floor material has no effect key or compiled effect after many frames, which identifies missing shader-side-effect registration in the modular Babylon import path as the scene readiness root cause.
- The correct StandardMaterial shader pair is `default.vertex` and `default.fragment`. Registering those modules explicitly restored the forest scene in the live preview, and the browser console now reports no active errors.
- The streamlined player overlay now shows warmth, the active Cinder Warden class, level, and a one-tap Satchel control. Browser testing confirmed the Satchel opens a focused drawer with inventory, item-use actions, and retained class skills while leaving the forest visible.
- Browser verification confirms the new Grove Map toggle opens a compact Whispergrove chart with the player, Hushling, Ember Shard, and Beacon route markers while preserving the primary scene and quest panel.
- Live keyboard testing confirms **I** opens the Satchel drawer and **Escape** closes it without affecting the independently opened Grove Map.
- After removing the GlowLayer path, the restarted browser session reported no shader errors. Full live combat testing then defeated the Hushling, awarded both drops, advanced the quest, and displayed the new animated `+2` Loot secured counter at the top of the forest scene.
- The autoplay quest run now advances the compact amber player marker across the opened Whispergrove minimap while Elian moves from the Hushling to the Ember Shard and beacon. It also displays the new loot counter for the Ember Shard award.
- Satchel verification confirms the Moss Tonic hover card renders its Common rarity, full description, and both stat lines: “Restores 12 warmth” and “Single-use remedy.”
- The expanded Whispergrove preview now visibly extends far beyond the original compact clearing, with a long pale-stone route, wider forest perimeter, and the Hushling set deeper into the first region.
- The scaled “Whispergrove expanse” minimap now separates the player, Hushling, Ember Shard, and beacon across the larger landmass. The autoplay route also reaches and engages the farther Hushling correctly under the expanded coordinates.
- Visual exploration checks confirm the 3D upgrade adds layered meadow islands, larger varied trees, fern clusters, route stones, rune waystones, and a moonrun stream; the camera now follows the player smoothly while preserving the isometric storybook composition.
- Movement and combat currently use immediate fixed-speed translation and a small root-level sine bob. The Cinder Warden and Hushling are procedural mesh groups without articulated sub-rigs, while cursor feedback is limited to static single-ring markers. The motion upgrade will keep Babylon and React decoupled by adding lightweight TransformNode-based rigs, velocity smoothing, and command pulses inside GameWorld’s frame loop.
- Motion direction selected: the Cinder Warden will accelerate toward a 6.4-unit/s walking pace, settle with soft deceleration, and turn with eased facing. Her rig will layer cloak sway, arm swing, lantern lift, walking bounce, attack lunge, Cinder Lash flare, and a short victory flourish. The Hushling will gain layered roots/tendrils, a breathing hover, targeted writhing, recoil flashes, lunge retaliation, and a visible collapse before removal. Cursor feedback will use expanding dual-rune pulses and directional chevrons, while target lock becomes a rotating teal sigil.
- Initial browser validation after the motion pass confirms that a grove click moves the Cinder Warden through the command layer without breaking input. The upgraded figure has visible lantern and sabre arms plus a fuller cloak silhouette; the browser frame tracks the movement smoothly. Combat and skill animations still require a separate interaction check.
- Fresh runtime validation showed the Hushling target marker and the Cinder Warden’s animated chase reaching an active auto-strike exchange; the enemy health dropped while the target-lock HUD remained correct, and the encounter proceeded to the normal loot/victory state. A Mend Flame activation restored warmth and placed the ability on its normal cooldown. The restarted preview reports no browser-console errors.
- A single immediate deterministic-demo capture briefly showed only the HUD before the Babylon scene had visibly settled. A direct fresh `?demo` browser pass resolved to the active grove, with the Cinder Warden and Hushling visible mid-exchange and the auto-strike HUD showing the Hushling at 12/28 health. This confirms the gameplay scene renders correctly once the demo’s initialization frame completes.
- The responsive-motion pass passed both `pnpm check` and the production build. The only build output is the pre-existing Babylon bundle-size advisory; no TypeScript or fresh browser-console errors remain.
- Hushling combat currently holds position while auto-strike closes to 1.42 units, retaliates only after player hits, and exposes hit/cast timers through GameWorld. Cinder Lash already has a dedicated cast timer and enemy-hit trigger, so the encounter extension can remain within GameWorld by adding a lightweight stateful movement controller and pooled procedural ember meshes—without coupling render effects to React or bringing in external assets.
- Encounter direction selected: the Hushling’s **Bramble Skitter** alternates an orbit around a 1.9-unit combat radius, a short sideways feint, and a cooldown-limited forward root-lunge that can land a small direct hit before recovering. Its movement stays bounded and intentionally leaves strike windows. Cinder Lash will trigger a 0.62-second impact bloom: an expanding amber ground sigil, a sharp central flash, and a pooled burst of ember shards that rise, arc outwards, fade, and return to standby.
- The first live engagement after implementation confirmed the Hushling moves away from its initial idle position during auto-strike, presents the lock-on rune correctly, and retains the normal combat/loot conclusion. Its faster behavior can allow the autonomous exchange to finish before an inventory skill is opened, so the Cinder Lash visual check is being performed from a freshly reset encounter with a direct controlled action.
- The first controlled Cinder Lash browser script successfully launched the encounter but missed the Satchel button because it matched its label case-sensitively; the natural auto-strike sequence then completed. The follow-up cast check will use a case-insensitive control lookup and run immediately after target selection, before observing the short impact effect.
- The corrected controlled cast fired Cinder Lash successfully: the live UI reported the exact Cinder Lash damage text and the Lash button immediately entered its six-second cooldown, proving the ember-impact trigger runs on a real active target. The Bramble Skitter encounter then completed through its normal reward path without runtime errors, confirming the new movement controller does not break the existing combat loop.
- The encounter enhancement passed `pnpm check` and the production build. A deterministic demo screenshot shows the Hushling visibly repositioned beside the Cinder Warden at 12/28 health with the auto-strike state active, confirming the moving encounter remains readable in the final visual pass.
